window.cipher = {
  encode: () => {
    /* Acá va tu código */
    for (let i = 0; i < txtCod.length; i++){
      let encode = txtCod.charCodeat (i);    
      const ascii = txtCod.charCodeat(i);
      const code = ascii + code;
      const result = String.fromCharCode(code); 
    };
  },
  decode: () => {
    /* Acá va tu código */
  }
}